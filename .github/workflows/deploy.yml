name: Deploy Frontend to GitHub Pages

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck (all)
        run: npm run typecheck:all

      - name: Tests
        run: npm test

      - name: Build
        run: npm run build
        env:
          VITE_WHITEBOARD_SERVER_BASE_URL: ${{ vars.VITE_WHITEBOARD_SERVER_BASE_URL }}
          VITE_OIDC_ISSUER: ${{ vars.VITE_OIDC_ISSUER }}
          VITE_OIDC_CLIENT_ID: ${{ vars.VITE_OIDC_CLIENT_ID }}
          VITE_OIDC_REDIRECT_URI: ${{ vars.VITE_OIDC_REDIRECT_URI }}
          VITE_OIDC_POST_LOGOUT_REDIRECT_URI: ${{ vars.VITE_OIDC_POST_LOGOUT_REDIRECT_URI }}
          VITE_OIDC_SCOPE: ${{ vars.VITE_OIDC_SCOPE }}
          # Public (safe to expose in a client build). Prefer GitHub "Variables" for these.

      - name: SPA fallback (GitHub Pages)
        run: cp dist/index.html dist/404.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
