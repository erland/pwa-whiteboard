# Example environment variables for pwa-whiteboard
# Copy to .env.local (recommended) or .env and fill values.
#
# NOTE: Never commit real secrets. The Supabase anon key is safe to use in the browser,
# but should still be treated as project configuration.

# Supabase (client-side)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Collaboration service (Cloudflare Worker/WebSocket base URL)
# Example: https://collab-dev.yourdomain.com
VITE_COLLAB_BASE_URL=https://YOUR_COLLAB_BASE_URL

# Optional: app mode toggles (future use)
# VITE_ENABLE_COLLAB=true
